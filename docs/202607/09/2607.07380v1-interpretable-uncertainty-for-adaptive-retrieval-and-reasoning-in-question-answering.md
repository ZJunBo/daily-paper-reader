---
title: Interpretable Uncertainty for Adaptive Retrieval and Reasoning in Question Answering
title_zh: 可解释的不确定性用于问答中的自适应检索与推理
authors: "Ritajit Dey, Iadh Ounis, Graham McDonald"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07380v1"
tags: ["query:luq"]
score: 9.0
evidence: 使用LLM内部表示的不确定性感知自适应RAG
tldr: 大型语言模型在问答中易产生幻觉，现有检索增强生成策略不透明且低效。本文提出基于内部表示的不确定性信号自适应框架，通过单次前向传播区分知识不足与冲突，分别触发检索或额外推理。该方法提升了透明度与效率，为设计可解释的问答系统提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07380v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 885, \"height\": 441, \"label\": \"Figure\"}]"
motivation: 现有LLM问答缺乏透明度且无法有效判断何时需要外部知识或推理。
method: 从LLM隐状态中提取分解的不确定性信号，分别指示知识不足与知识冲突，指导自适应检索与推理。
result: 在单次前向传播中完成不确定性估计，实现高效的检索与推理决策。
conclusion: 提出了一种透明实用的自适应策略，支持可解释的问答系统构建。
---

## 摘要
大型语言模型（LLMs）在问答（QA）中表现出强劲性能，但仍易产生幻觉且透明度有限。检索增强生成（RAG）可提升事实准确性，但关于何时以及如何从外部资源进行检索的决策通常基于不透明策略或计算效率低下的多步提示流程。我们提出一种基于显式信号的不确定性感知框架，用于自适应问答，这些信号源自LLM内部表示。我们区分知识不足与知识模糊或冲突，并通过单次前向传播从隐藏状态中高效估计这些信号。这些信号指导系统行为：当知识不足时触发RAG，而当模糊或冲突程度高时应用额外推理。通过将自适应决策建立在分解且可高效估计的不确定性信号之上，该方法为现有检索与推理策略提供了一种透明且实用的替代方案，支持可解释性用户面向工具的设计。

## Abstract
Large language models (LLMs) achieve a strong performance in question answering (QA), but remain prone to hallucinations and suffer from limited transparency. Retrieval-augmented generation (RAG) can improve factuality, yet decisions about when and how to retrieve from external resources are typically based on opaque policies or computationally inefficient multi-step prompting procedures. We propose an uncertainty-aware framework for adaptive QA based on explicit signals derived from LLM internal representations. We distinguish between knowledge insufficiency and knowledge ambiguity or conflict, and efficiently estimate these from hidden states in a single forward pass. These signals guide system behaviour: RAG is triggered when knowledge is insufficient, while additional reasoning is applied when ambiguity or conflict is high. By grounding adaptive decisions in decomposed and efficiently estimable uncertainty signals, this approach provides a transparent and practical alternative to existing retrieval and reasoning strategies supporting the design of interpretable user-facing tools.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机与背景）

- **核心问题**：大型语言模型（LLM）在问答（QA）中虽然表现强劲，但存在**幻觉**和**透明度不足**的问题。现有的检索增强生成（RAG）策略在决定“何时”及“如何”检索时，要么依赖**不透明的隐式策略**（如端到端学习），要么依赖**计算低效的多步提示流程**（如Self-RAG），导致系统行为难以解释，且无法区分不同来源的模型不确定性。

- **整体含义**：作者主张将自适应QA的决策基础从“黑盒策略”转向**显式、可解释的不确定性信号**，从而构建既透明又高效的问答系统。通过区分**知识不足**（模型缺乏所需事实）与**知识模糊/冲突**（模型有多个竞争性答案），可分别触发**外部检索**或**额外推理**，为用户和系统设计师提供可理解的决策依据。

## 2. 方法论：核心思想、关键技术细节与流程

- **核心思想**：利用LLM**单次前向传播**的隐藏状态（hidden states）来高效估计两种不确定性信号，并基于阈值触发不同行为。

- **关键技术细节**：
  - **不确定性分解**：
    - **知识不足（Knowledge Insufficiency）**：定义为该事实在LLM预训练语料中的**出现次数**，次数越少，表示模型越可能缺乏该知识。通过训练一个**回归探针（regression probe）**，从隐藏状态预测该次数。
    - **知识模糊/冲突（Knowledge Ambiguity/Conflict）**：定义为不同版本事实在预训练语料中出现的**熵**（entropy），熵越高说明多个候选答案竞争激烈，模型不确定性高。同样通过回归探针从隐藏状态预测该熵值。
  - **自适应决策机制**（见Figure 1）：
    - 输入问题到LLM，提取隐藏状态，经探测预测两个不确定性分数。
    - 若**知识不足**超过阈值 → 触发**RAG**（外部检索补充信息）。
    - 若**模糊/冲突**超过阈值 → 触发**额外推理**（如Chain-of-Thought、Self-Consistency等增加测试时计算）。
    - 若两者都低 → 直接由LLM回答。
  - **效率优势**：所有不确定性估计只需**单次前向传播**，无需多次采样或辅助模型，适合多步/智能体场景。

## 3. 实验设计

- **数据集**：使用**NQ（Natural Questions）数据集**，这是一个常用的事实性问答基准。
- **模型**：**Llama-2-7b-chat**（7B参数聊天版）。
- **对比方法**：
  - **LLM-only基线**：仅使用LLM直接回答，不检索。
  - **Always-on RAG基线**：对每个问题都进行检索增强生成。
  - **自适应不确定性方法**：使用两种不确定性信号——**Semantic Entropy (SE)** 和 **Weighted Entropy Production Rate (WEPR)**——来选择性触发RAG。
- **评估指标**：未明确说明，但给出了绝对性能提升百分比，并采用**McNemar检验**验证统计显著性。
- **结果概要**：
  - 使用SE时：优于LLM-only +5.9%，优于Always-on RAG +3.3%。
  - 使用WEPR时：优于LLM-only +4.7%，优于Always-on RAG +2.1%。
- **实验覆盖**：仅有一个数据集、一个模型、两种不确定性信号，未进行消融实验或在不同模型/数据集上验证。

## 4. 资源与算力

- **文中未明确提及**使用的GPU型号、数量或训练时长。只提到模型为Llama-2-7b-chat，属于中等规模开源模型，训练探针（回归探针）需要额外资源，但具体算力开销未披露。

## 5. 实验数量与充分性

- **实验数量**：仅一组主要实验（NQ数据集，两个不确定性度量），无消融实验、无跨模型/跨数据集验证。
- **充分性评价**：**初步且有限**。虽然结果展现了潜力，但实验覆盖范围窄：
  - 未测试不同规模的LLM（如13B、70B）。
  - 未在不同类型的问题（长尾、时间敏感、多跳推理等）上评估。
  - 未与目前主流自适应RAG方法（如Self-RAG、Adaptive-RAG）进行直接比较（文中提到的对比基线只有LLM-only和Always-on RAG）。
  - 未消融探针设计（如不同隐藏层、不同训练数据规模的影响）。
  - 未验证模糊/冲突信号触发额外推理的有效性（实验中似乎只验证了知识不足信号触发RAG，未测试冲突信号触发推理）。
- **客观性**：作者明确指出这是“初步结果”（Preliminary Results），符合论文的定位（WExIR 2026短论文/研讨会论文）。

## 6. 主要结论与发现

1. 将不确定性划分为**知识不足**和**知识模糊/冲突**，可分别指导不同的干预措施（检索 vs. 推理），为自适应QA提供**可解释的决策基础**。
2. 利用LLM隐藏状态的**单次前向传播**估计两种不确定性是**可行的**，避免了多步提示或多次采样带来的计算开销。
3. 在NQ数据集上，基于不确定性信号（SE/WEPR）选择性触发RAG，显著优于**纯LLM**和**始终RAG**两个基线，证明了“适度检索”策略的有效性。
4. 该方法有望为**可解释的用户面向工具**提供设计原则，使用户和开发者能直接理解系统为何进行检索或推理。

## 7. 优点

- **概念创新**：首次明确区分两种不确定性源并对应不同行动，比单一不确定性评分更细致、更可解释。
- **效率优秀**：单次前向传播即可获得决策信号，避免了现有方法（如Self-RAG）的多步推理成本。
- **透明性设计**：从模型内部表示直接提取信号，决策可审计、可解释，有助于建立用户信任。
- **实验验证**：初步结果在统计上显著优于两个强基线，证明了自适应策略的潜力。

## 8. 不足与局限

- **实验有限**：仅在一个数据集和一个模型上进行验证，缺乏跨领域、跨模型的泛化性测试。未与其他主流自适应RAG方法（如Self-RAG, Adaptive-RAG, REPOFORMER）进行直接比较。
- **冲突信号未验证**：论文提出了“冲突/模糊时触发推理”的机制，但在实验中只测试了触发RAG的场景（知识不足信号），未验证触发推理的效果。
- **探针训练依赖**：回归探针需要预训练语料的“出现次数”和“熵”作为监督信号，获取这些真实标签可能困难，且探针本身可能引入额外偏差。
- **阈值设置问题**：文中未讨论如何设定决策阈值，实际应用中阈值可能需要调参或动态调整，但未给出指导。
- **资源开销未说明**：训练探针所需的数据、时间、算力未提供，影响可复现性评估。
- **论文形式**：属于短论文/定位论文（3页），内容密度有限，未提供完整的技术细节和广泛的实验支撑。

（完）
