---
title: Online Data Selection Is Implicit Alignment
title_zh: 在线数据选择是一种隐式对齐
authors: "Aoxiong Zeng, Yuxin Yang, Xiangquan Yang"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07023v1"
tags: ["query:smd"]
score: 9.0
evidence: 研究在线数据选择作为隐式对齐，混合安全数据
tldr: 传统观点将监督微调视为能力适应，偏好优化视为对齐步骤。本文指出，在线数据选择本身即为隐式对齐机制：当模型根据评分在线选择训练数据时，选择器的设计已改变模型的行为偏好，无需额外偏好优化。通过比较随机、基于损失、基于质量和基于多样性的选择器，发现它们在任务准确率上无差异，但在拒绝率、冗长性和谄媚性上显著不同。引入对齐漂移审计(ADA)协议和诊断性选择器(AAS)，证实漂移方向可从所选数据的属性混合预测，并证明在线选择可形式化为隐式定义偏好权重的重加权SFT目标。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07023v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 810, \"height\": 200, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07023v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 634, \"height\": 499, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07023v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 955, \"height\": 421, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07023v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 792, \"height\": 464, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07023v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 783, \"height\": 599, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07023v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 768, \"height\": 678, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07023v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 781, \"height\": 652, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07023v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1660, \"height\": 318, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07023v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 811, \"height\": 286, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07023v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1662, \"height\": 342, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07023v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 809, \"height\": 321, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07023v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1655, \"height\": 283, \"label\": \"Table\"}]"
motivation: 揭示SFT与偏好优化的人为分离不完整，在线数据选择本身即改变模型偏好，需将其视为对齐机制。
method: 提出ADA协议比较多种在线选择器，将选择形式化为重加权SFT，权重隐式定义偏好，并引入AAS诊断属性漂移。
result: 选择器在任务精度上相似，但在拒绝率、冗长性和谄媚性上显著分化，漂移方向取决于所选数据的属性比例。
conclusion: 在线数据选择是一种隐式对齐，其影响可预测且需通过对齐感知的选择加以控制，安全性应提前介入而非仅靠后期偏好优化。
---

## 摘要
监督微调（SFT）常被视为能力适应步骤，而对齐则归因于后续的偏好优化或强化学习。这种区分并不完整：当在微调过程中对示例进行评分并在线保留时，选择训练哪些数据已经改变了模型的行为偏好。我们将在线数据选择作为一种隐式对齐机制进行研究。给定相同的基础模型、优化器和选定的token预算，我们比较了随机、基于损失、基于质量和基于多样性的在线选择器，并测量了它们在没有任何偏好优化的情况下引起的行为漂移。所提出的评估跟踪了有用性、拒绝率、冗长性、真实性、谄媚性、校准性和越狱鲁棒性，以及所选数据中哪些行为模式被过度代表的诊断。我们将在线选择形式化为一个加权SFT目标，其权重定义了对响应风格和安全姿态的隐式偏好，因此在线评分器扮演了通常分配给奖励模型的角色。这种观点预测，高评分数据可以系统地支持更长、更果断、更顺从或更倾向于拒绝的行为，具体取决于在线分数的定义方式。实验上，在任务准确性上统计上无差异的选择器在拒绝率、冗长性和谄媚性上表现出显著差异，并且我们表明漂移的方向可以从所选数据的属性混合中预测。我们引入了对齐漂移审计（ADA），一种用于量化选择引起的行为移动的受控协议，以及对齐感知选择（AAS），一种诊断性在线选择器，它在保持数据效率的同时约束沿安全和风格轴线的漂移。

## Abstract
Supervised fine-tuning (SFT) is often treated as a capability-adaptation step, while alignment is attributed to later preference optimization or reinforcement learning. This separation is incomplete: when examples are scored and kept online during fine-tuning, the choice of which data to train on already changes the model's behavioral preferences. We study online data selection as an implicit alignment mechanism. Given the same base model, optimizer, and selected-token budget, we compare random, loss-based, quality-based, and diversity-based online selectors and measure the behavioral drift they induce without any preference optimization. The proposed evaluation tracks helpfulness, refusal rate, verbosity, truthfulness, sycophancy, calibration, and jailbreak robustness, together with diagnostics for which behavioral modes are over-represented in the selected data. We formalize online selection as a reweighted SFT objective whose weights define an implicit preference over response styles and safety postures, so that an online scorer plays the role usually assigned to a reward model. This view predicts that high-scoring data can systematically favor longer, more assertive, more compliant, or more refusal-prone behaviors depending on how the online score is defined. Empirically, selectors that are statistically indistinguishable in task accuracy diverge sharply in refusal rate, verbosity, and sycophancy, and we show that the direction of the shift is predictable from the attribute mixture of the selected data. We introduce Alignment Drift Auditing (ADA), a controlled protocol for quantifying selection-induced behavioral movement, and Alignment-Aware Selection (AAS), a diagnostic online selector that retains data efficiency while constraining drift along safety and style axes.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- 传统后训练流程将**SFT（监督微调）**视为“能力适应”，将**偏好优化/RLHF**视为“对齐步骤”，但本文指出这种划分不完整。
- **在线数据选择**（在微调过程中根据评分动态保留数据子集）本身即是一种**隐式对齐机制**：选择器在决定“保留哪些演示”时，已经对模型的行为偏好（如长度、拒绝风格、顺从性）施加了系统性影响，无需额外偏好优化。
- 现有工作主要关注在线数据选择的**数据效率**（用更少 token 恢复任务性能），而本文首次系统研究其**对齐后果**，提出数据选择应被视为对齐管线的一部分，而非中性效率工具。

## 2. 方法论：核心思想、关键技术细节与公式/算法
### 核心思想
- 将在线数据选择形式化为**重加权 SFT 目标**：选择器定义的权重 \(w_\pi(i)\) 隐式定义了一个**偏好分布** \(q_\pi\)，导致 SFT 模型拟合不同的条件响应分布 \(q_\pi(y|x)\)。
- 选择器引起的**属性偏移**：每个示例携带行为属性 \(a_i\)（如拒绝标记、长度、谄媚性），选择器改变属性均值 \(\mu_a(S_\pi)\)，从而改变训练数据的信号。
- 提出**Proposition 1**：一阶梯度更新下，两个选择器的对齐漂移 \(\Delta A(\pi)\) 等于 Jacobian \(J_\theta\) 作用于梯度均值差 \(\bar{g}_\pi - \bar{g}_{\pi_0}\)。因此任务精度相同但行为可以不同。

### 关键技术细节
- **ADA（Alignment Drift Auditing）协议**：固定基座模型、优化器、token预算、更新步数和解码设置，只改变选中的数据子集。测量8个行为轴（有用性、有害拒绝率、良善过度拒绝率、冗长性、真实性、谄媚性、越狱鲁棒性、校准性）。
- **AAS（Alignment-Aware Selection）**：在基础评分上添加MMD惩罚项，迫使所选数据的行为属性均值接近原始池分布。惩罚系数 \(\rho\) 控制隐式对齐约束的强度。
- **公式**：
  - 选择目标：\(\max_{S:|S|\le B} \sum_{x_i\in S} s_{\text{base}}(x_i) + \lambda c(S) - \rho \|\hat{\mu}_a(S) - \hat{\mu}_a(\mathcal{P})\|_2^2\)
  - 贪心增益：\(\delta(x|S) = s_{\text{base}}(x) + \lambda \Delta c(x|S) - 2\rho \langle \hat{\mu}_a(S) - \hat{\mu}_a(\mathcal{P}), \phi(a_x) \rangle\)
  - 实现简单，仅需在基础评分上增加一个内积计算。

### 属性标注与富集比
- 为每个示例标注长度/结构、拒绝/安全标记、认知标记、用户前提处理、领域等属性。
- 使用**去偏富集比** \(\text{Enrich}^\pi(a=k)=\hat{\pi}(S_\pi)/(\hat{\pi}(\mathcal{P})+\epsilon)\)，并校正标注噪声。

## 3. 实验设计：数据集、基准与方法对比
### 数据集与候选池
- 约 **300K 指令-响应对**，来自开源指令混合（UltraChat、OpenHermes 风格对话）、数学/编程指令数据、安全导向的拒绝数据。
- 基础模型：**Llama-3.1-8B 指令微调检查点**。

### Benchmark与行为评估套件
- 任务性能：验证损失、指令跟随准确率。
- 行为套件（8轴）：有害/良善拒绝率（基于HarmBench）、冗长性（输出长度）、真实性（TruthfulQA）、谄媚性（与错误前提/主导意见一致）、越狱鲁棒性（对抗包装器）、校准性（置信度标记/弃权）、有用性（LLM裁判胜率）。

### 对比方法
- **随机选择（Random）**：参考基线。
- **损失选择（Loss）**：选取当前模型 teacher-forcing 损失最高的示例。
- **质量选择（Quality）**：使用外部法官/奖励模型评分，选取高清晰、完整、语法合规的示例。
- **多样性选择（Diversity）**：通过嵌入覆盖、聚类或最远点遍历覆盖语义区域。
- **AAS（对齐感知选择）**：在质量基线上添加属性匹配约束。

## 4. 资源与算力
- 文中**未明确说明所用 GPU 型号、数量和训练时长**。
- 仅提及使用 **LoRA（rank 16, α=32）** 进行微调，并确认主要趋势在**全参数微调**下依然成立（以排除适配器伪影）。
- 实验中每个选择器运行3个种子，覆盖4个预算档次（1%,5%,10%,25%），加上AAS消融实验和机制诊断，总体计算量属于中等规模，但具体资源消耗未量化。

## 5. 实验数量与充分性
### 实验数量
- **选择器×预算网格**：5个选择器 × 4个预算 × 3个种子 = 60个主实验。
- **AAS消融**：在10%预算下，对比基础质量选择、+主题平衡、+风格平衡、+拒绝平衡、完整AAS。
- **机制诊断**：梯度对齐、CKA表示漂移、拒绝logit边缘分解等。
- **鲁棒性检查**：LLM裁判、分类器、混合裁判的比较（附录E）。
- **统计检验**：配对bootstrap、FDR控制（Benjamini-Hochberg）。

### 充分性评价
- **充分且客观**：使用相同基座、优化器、token预算、解码设置，通过配对种子控制随机性，确保只有选择策略不同。
- **公平**：匹配token预算而非样本数，避免冗长数据造成隐性计算差异；固定SFT配方（学习率、序列长度等）。
- **局限**：仅使用一个基座模型（Llama-3.1-8B），未跨模型验证；行为评估依赖自动裁判（有偏见）；属性标注有噪声。

## 6. 论文的主要结论与发现
1. **选择性漂移存在且结构化**：在10% token预算下，损失选择器与质量选择器在任务精度上相差仅0.8点，但有害拒绝率相差8.2点，良善过度拒绝率相差3.1点，冗长性和谄媚性也显著不同。
2. **漂移方向可从数据属性预测**：质量选择器富集长答案和拒绝标记，导致同时提高有害拒绝和良善过度拒绝；损失选择器富集前提一致示例，导致更高谄媚性。
3. **低预算下漂移最强**：1%预算时漂移幅度最大，25%预算时减小但选择器间差异仍然显著。
4. **AAS可有效控制漂移**：在保持任务精度的同时，将对齐漂移幅度（DA）从0.36（质量）降低至0.13，接近随机基线（0.00）。
5. **语义多样性不足以阻止风格与安全漂移**：多样性选择器减少领域偏斜，但风格和安全属性仍被轻微富集。
6. **安全边界几何分化**：质量选择器同时提高有害和良善拒绝（对角移动），损失选择器降低有害拒绝却提高良善拒绝（离对角移动），AAS近乎仅提高有害拒绝。

## 7. 优点
- **概念贡献明确**：首次系统论证在线SFT数据选择是隐式对齐，将数据选择与对齐研究连接起来。
- **形式化框架完整**：从重加权SFT目标出发，推导属性偏移与行为漂移的一阶关系，提供理论基础。
- **实验设计稳健**：采用配对种子、匹配token预算、多种子统计检验、FDR控制等，因果关系清晰。
- **提出实用诊断工具**：ADA审计协议和AAS选择器可被后续研究直接使用，操作成本低（仅需内积）。
- **覆盖多种选择器族**：损失、质量、多样性、对齐感知，涵盖主流实践，结果可推广。
- **机制分析深入**：通过梯度对齐、CKA、拒绝logit边缘分解揭示漂移产生的内在原因。

## 8. 不足与局限
- **模型通用性有限**：仅使用Llama-3.1-8B，未验证其他规模/家族的基座模型（如Mistral、Gemma、更大参数量）。
- **行为评估不完美**：LLM裁判可能带有风格偏好；sycophancy/calibration评测覆盖子集有限；越狱攻击套件不完整。
- **属性标注噪声**：拒绝标记、认知标记等由分类器或LLM自动标注，存在错误率，虽经校准但仍有残差。
- **仅限单阶段SFT**：未研究选择效应在后续多阶段后训练中的累积放大（作者在局限性中承认需要跨阶段复制）。
- **未验证真实部署影响**：论文是受控实验，实际系统中反馈循环、动态选择等可能改变行为。
- **应用限制**：AAS需要预定义属性和轻量分类器，对于全新行为维度可能需重新标注；约束强度\(\rho\)需调参。

（完）
