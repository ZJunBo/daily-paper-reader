---
title: LoRA for Gender-Inclusive Rewriting and Activation Steering for Counter-Narrative Generation
title_zh: 用于性别包容性改写与反叙事生成的LoRA及激活引导
authors: "Akhil Rajeev P, Manoj Balaji J"
date: 2026-07-25
pdf: "https://arxiv.org/pdf/2607.23083v1"
tags: ["query:dg"]
score: 8.0
evidence: 利用PCA从对比隐藏状态中提取方向并进行激活引导，用于反叙事生成
tldr: "针对性别偏见文本，该工作提出性别包容改写与反叙事生成系统。采用LoRA微调实现改写，并创新性地利用PCA从对比激活中提取引导方向，在推理时注入Gemma-3-4B-it中间表示，实现无需权重更新的行为引导。官方分数分别为80.00%和78.12%，并分析了语义漂移、残留偏见等失败模式，展示激活引导作为轻量级可控生成方案的潜力与局限。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23083v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 815, \"height\": 443, \"label\": \"Table\"}]"
motivation: 性别包容改写需要保留语义且调整偏见，但传统全量微调成本高，需探索高效的参数更新或推理时干预方法。
method: 用LoRA微调做改写；用PCA从对比隐藏状态提取主引导方向，推理时注入Gemma-3-4B-it中间表示，结合约束提示生成反叙事。
result: "改写任务官方得分80.00%，反叙事生成78.12%，并人工分析出语义漂移、过引导等主要失败模式。"
conclusion: 激活引导是一种轻量且有效的可控生成替代方案，但在稳定性和偏见消除上仍需改进。
---

## 摘要
性别包容性语言生成旨在将有偏见的文本转化为包容性替代文本，同时保持语义和上下文连贯性。本文介绍了针对LT-EDI 2026共享任务的IHLC系统，同时处理性别包容性改写和反叙事生成。对于性别包容性改写，我们采用了参数高效的低秩适配（LoRA）微调，官方得分为80.00%。我们的主要贡献是一种计算高效的推理时表示工程方法，用于反叙事生成。我们通过主成分分析（PCA）从对比隐藏状态激活中推导出主引导方向，并在推理时将其注入Gemma-3-4B-it的中间表示，从而在不修改模型权重的情况下实现向包容性响应的行为引导。结合约束提示，该方法能生成礼貌且上下文恰当的反叙事，官方得分为78.12%。我们还对引导行为进行了手动分析，识别出关键失败模式，包括语义漂移、残余偏见泄漏、层敏感性、过度引导和文本退化。我们的研究结果突显了激活引导作为参数更新的轻量级替代方案在可控和社会对齐语言生成中的实际潜力与当前局限性。

## Abstract
Gender-inclusive language generation seeks to transform biased text into inclusive alternatives while preserving semantic meaning and contextual coherence. This paper presents the IHLC system for the LT-EDI 2026 Shared Task, addressing both gender-inclusive rewriting and counter-narrative generation. For gender-inclusive rewriting, we employ parameter-efficient Low-Rank Adaptation (LoRA) fine-tuning, achieving an official score of 80.00%. Our primary contribution is a compute-efficient inference-time representation engineering approach for counter-narrative generation. We derive a principal steering direction from contrastive hidden-state activations using principal component analysis (PCA) and inject it into the intermediate representations of Gemma-3-4B-it during inference, enabling behavioral steering toward inclusive responses without modifying model weights. Combined with constrained prompting, this approach produces polite and contextually appropriate counter-narratives, achieving an official score of 78.12%. We further present a manual analysis of steering behavior, identifying key failure modes including semantic drift, residual bias leakage, layer sensitivity, over-steering, and text degeneration. Our findings highlight both the practical potential and current limitations of activation steering as a lightweight alternative to parameter updates for controllable and socially aligned language generation.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：性别偏见在语言模型中普遍存在并被放大，性别包容性语言生成旨在将有偏见的文本转化为包容、中性且语义连贯的替代文本。然而，传统的全量微调方法计算成本高，且难以实现细粒度的行为控制，迫切需要探索更高效的参数更新或推理时干预方案。
- **任务背景**：本文是IHLC团队针对 **LT-EDI 2026共享任务** 的提交系统，该任务包含两个子任务：
  - **子任务A**（性别包容性改写，仅英语）：将性别标记或有偏见的句子改写为完全包容的变体（如 fireman → firefighter）。
  - **子任务B**（反叙事生成，仅英语）：针对明显的性别偏见陈述，生成富有同理心、有说服力的纠正性反叙事。
- **整体含义**：论文探索了两种不同技术路线——LoRA参数高效微调与激活引导（Activation Steering）推理时干预——用于实现性别包容性文本生成，并系统分析了激活引导方法在实践中的潜力与局限，为轻量级、可控的模型行为对齐提供了实证参考。

## 2. 论文提出的方法论

### 2.1 子任务A：LoRA微调

- 采用 **Low-Rank Adaptation (LoRA)** 对模型进行参数高效微调，仅更新低秩适配矩阵，避免全量微调的高昂计算成本，用于将性别标记句子改写为中性版本。

### 2.2 子任务B：激活引导（Activation Steering）

这是论文的核心贡献，一种**无需权重更新**的推理时干预方法：

1. **引导方向提取**：
   - 从配对的偏置句子（负样本）和包容性改写（正样本）中，提取指定Transformer层的隐藏激活。
   - 计算配对样本间的逐示例激活差异，对差异向量执行**主成分分析（PCA）**，取第一主成分作为引导方向（steering direction）。
2. **推理时注入**：
   - 在选定中间层（实验中为 L=16）注册前向钩子（forward hook），将引导向量乘以缩放系数 α（实验中推荐 0.6–1.2）后加到该层所有标记位置（或最后标记）的隐藏状态上，从而在推理时引导模型输出偏向包容性方向。
3. **提示模板与解码策略**：
   - **DEI软提示**：声明模型为DEI改写专家并提供示例，适合灵活、解释性输出。
   - **严格提示**：限定模型仅输出单行最终句，避免解释性前缀干扰自动评测。
   - 解码采用**低温度采样 + 重复惩罚**，防止高引导系数导致的文本退化和循环。

## 3. 实验设计

- **数据集**：
  - 使用LT-EDI 2026组织者提供的英语子任务A句对数据和子任务B反事实配对数据，具体规模在任务文档中说明。
- **基准与评估**：
  - 官方采用 **LLM-as-a-judge + 专家人工抽检**的混合评估框架。
  - 子任务A：GA（性别假设消除）、GN（性别中立性）、QR（质量与相关性），综合得分为三者均值。
  - 子任务B：PR（礼貌与尊重）、CCNC（上下文反叙事连贯性）、QR（质量与相关性），综合得分为三者均值。
- **对比方法**：
  - 子任务A未能说明是否与基线对比；子任务B也没有与传统微调、全量微调等基线进行系统比较，主要是与官方排行榜上的其他参赛团队做间接对比。
- **模型**：Gemma-3-4B-it，基于PyTorch/HuggingFace实现。

## 4. 资源与算力

- **论文明确说明**：选择激活引导的动机之一就是克服微调的计算资源限制，其本身属于计算高效的方法。
- **但论文未报告具体算力信息**：全文没有提及GPU型号、GPU数量、训练时长、推理耗时等量化指标，无法评估方法的实际资源开销。

## 5. 实验数量与充分性

- **实验组数有限**：
  - 子任务A和B各仅有一组官方提交结果，没有针对LoRA超参数（如rank、α等）的消融实验。
  - 子任务B对47个输出进行了人工评估分析，并做了尝试性实验（trial-error），覆盖引导强度、提示模板、解码策略三个维度的调优。
- **充分性评价**：
  - **不足**：缺乏对不同引导层（除L=16外）、不同引导系数（仅给出推荐范围而非系统扫描）、不同PCA分量数量的系统消融；缺乏与权重更新方法（如全量微调、指令微调）的公平对比；样本量较小（47个案例），统计功效有限。
  - **客观性**：官方评估采用混合评估框架并有人工监督，具备一定的客观性，但人工评估本身为本文作者自行进行，存在主观偏差风险。

## 6. 主要结论与发现

- **子任务A**：LoRA微调在GA、GN、QR三项上均得80.00%，综合80.00%，在9支参与队伍中排名第3，表明LoRA能稳定实现中性词汇选择和语义保持。
- **子任务B**：综合得分78.12%（官方排名第6/7），其中PR（84.89%）和CCNC（84.79%）表现良好，说明系统能产出礼貌且上下文相关的反叙事；但QR（64.68%）明显拉低总分，提示存在语义漂移或相关性下降。
- **失败模式分析（n=47）**：
  - **保真-编辑权衡**（53%）：输出过度模板化（如一律以“People of all gender identities...”开头），偏离最小编辑范式。
  - **层特异性纠缠**（23%）：单层引导无法彻底解耦偏见相关概念，负性特质被转化为相近语义簇（如“naive”→“innocence and vulnerability”）。
  - **残留偏见泄漏**（11%）：仅作用于最后标记表示，未能消除早期上下文token中编码的偏见，否定性框架被保留。
  - **过引导不稳定**（9%）：高α值导致模型完全脱离输入句法结构，转向泛化的社会评论。
  - **重复与退化**（4%）：强扰动破坏tokenizer的分词逻辑，造成词拼接与文本退化。

## 7. 优点

- **方法创新性**：将激活引导技术引入性别包容性反叙事生成这一社会敏感任务，拓展了表示工程的应用场景，是推理时干预用于社会对齐的积极探索。
- **计算高效**：无需权重更新即可实现可控的行为偏置，对计算资源受限的团队具有实际参考价值。
- **方法互补**：LoRA微调与激活引导分别覆盖改写和生成两个子任务，展示了从参数更新到推理时干预的完整技术谱系。
- **诚实的失败分析**：对47个输出进行了细致的人工错误分类，量化和解释了多种失败模式，有助于后续研究针对性改进，而非仅报告分数。
- **可复现性**：代码和实验过程以Jupyter notebook形式公开在GitHub上。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅参与英语，缺乏多语言验证，无法考察不同语言中性别表达的文化特异性。
  - 子任务B仅对比了官方排行榜名次，缺少与基线方法（如全量微调、RAG+CoT）的系统对照实验。
  - 未提供消融实验（如不同引导层、不同α的系统扫描、不同解码策略的量化对比），调优过程依赖trial-error，科学严谨性有限。
- **方法固有局限**：
  - 单层（L=16）、最后标记（last-token）的引导假设偏置线性可编码，实际中偏置与多个语义簇纠缠，导致部分概念无法被干净地中和。
  - 激活引导在α设置不当时容易产生语义漂移、过度泛化和文本退化，稳定性不足，需要精确调参和额外的后处理。
  - 只能缓解表面形式的偏见，无法保证深层因果结构的修正（如残留否定框架）。
- **评估与偏差风险**：
  - 人工评估仅由作者自评，无独立评分者间一致性检验（如Kappa系数）。
  - LLM-as-a-judge范式本身存在偏见和局限性，论文也承认人工监督在检测细微语义漂移时仍不可替代。
  - 测试样本量小（47个），结论的外推性有限。
- **资源透明度**：未报告GPU型号、训练时间等，影响可复现性和算力需求的客观评估。

（完）
