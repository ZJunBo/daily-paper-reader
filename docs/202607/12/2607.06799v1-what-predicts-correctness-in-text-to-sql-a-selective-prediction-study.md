---
title: What Predicts Correctness in Text-to-SQL? A Selective-Prediction Study
title_zh: 文本到SQL的正确性预测：一项选择性预测研究
authors: Robert Richardson
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06799v1"
tags: ["query:luq"]
score: 8.0
evidence: 研究预测正确性的信号，包括自一致性等不确定性度量
tldr: 文本到SQL任务中，预测查询正确性需判断其执行结果是否与人工标注一致。本研究在BIRD和Spider数据集上评估多种不确定性信号，发现基于自洽性的信号AUROC约0.61-0.68，而基于LLM评判者的信号可达0.72-0.78，双提供者集成达0.82且校准良好。自洽性无法提供低风险子集，而验证信号支持有效弃权。微调验证器在分布内表现良好（0.77-0.79），但跨模式泛化失败。研究表明，正确性不确定性依赖于推理信号，跨模式泛化需大型推理模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06799v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 814, \"height\": 391, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06799v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1680, \"height\": 569, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06799v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1331, \"height\": 333, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06799v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1367, \"height\": 162, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06799v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1297, \"height\": 246, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06799v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1479, \"height\": 289, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06799v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 867, \"height\": 246, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06799v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1069, \"height\": 203, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06799v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1404, \"height\": 420, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06799v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1161, \"height\": 386, \"label\": \"Table\"}]"
motivation: 现有方法缺乏对文本到SQL查询正确性的可靠预测，需要系统评估不同信号在不确定性估计中的效能。
method: 使用AUROC指标评估黑盒信号（字符串、结构和执行自洽性等）和白盒信号（对数概率），并测试LLM评判者及微调验证器，进行跨模式泛化分析。
result: LLM评判者AUROC达0.72-0.78，双提供者集成达0.82，校准误差0.03；微调验证器分布内0.77-0.79，但跨模式跌至0.66。
conclusion: 正确性预测依赖推理信号，跨模式泛化需依赖大型冻结推理模型而非微调，实用中建议使用LLM评判者集成。
---

## 摘要
评估AI生成的SQL查询的不确定性需要估计查询是否正确，其中正确意味着它执行的结果与人工编写的参考查询相同。我们研究了哪些信号能够预测复杂多表文本到SQL的正确性，使用AUROC来衡量每个信号将正确查询排在错误查询之上的能力。在BIRD和Spider数据集上，黑盒信号如字符串、结构和执行自洽性、模式相关性分数以及查询可执行性，其AUROC大约在0.61到0.68之间，其中字符串自洽性最强（0.675）；白盒对数概率也类似（0.67）。能够超越这一上限的信号是基于验证的：LLM裁判（GPT-4o-mini）得分从0.72到Claude的0.78。不同提供商的裁判会犯不同的错误，因此双提供商的集成可达到0.82 AUROC，具有良好校准的概率（预期校准误差0.03），并支持有用的弃权边界（例如，以24%的选择性风险回答27%的问题），而自洽性无法提供有效的低风险子集。这一模式在两个基准、两个生成器以及两个裁判提供商上保持一致。我们还询问是否能够训练一个验证器。微调的验证器（包括编码器和生成器）在分布内可达到约0.77到0.79 AUROC，但在未见模式上降至约0.66；扩展到7B、增加模式多样性、蒸馏强裁判的推理过程以及跨基准训练都无法缩小这一差距。跨模式迁移似乎追踪模型规模和推理能力，而非微调。在实践中，文本到SQL的正确性不确定性存在于基于推理的信号中：微调的验证器是一个良好的领域内工具，但能够跨模式泛化的验证器目前意味着一个大型冻结的推理模型。

## Abstract
Evaluating uncertainty in AI-generated SQL queries requires estimating whether a query is correct, where correct means it executes to the same result as a human-written reference. We study which signals predict correctness on hard multi-table text-to-SQL, using AUROC to measure how well each ranks correct queries above incorrect ones. On BIRD and Spider, black-box signals such as string, structural, and execution self-consistency, a schema-relevance score, and query executability all fall between about 0.61 and 0.68 AUROC, with string self-consistency strongest at 0.675; white-box log-probability is similar (0.67). The signals that move past this ceiling are verification-based: an LLM judge scores from 0.72 (GPT-4o-mini) to 0.78 (Claude). Judges from different providers make different errors, so a two-provider ensemble reaches 0.82 AUROC with a well-calibrated probability (expected calibration error 0.03) and supports useful abstention frontiers (for example, answering 27% of questions at 24% selective risk) where self-consistency offers no valid low-risk subset. The pattern holds across two benchmarks, two generators, and two judge providers. We also ask whether a verifier can be trained. Fine-tuned verifiers, both encoder and generative, reach about 0.77 to 0.79 AUROC in-distribution but fall to about 0.66 on unseen schemas; scaling to 7B, adding schema diversity, distilling a strong judge's rationales, and cross-benchmark training all fail to close that gap. Cross-schema transfer appears to track model scale and reasoning rather than fine-tuning. In practice, correctness uncertainty for text-to-SQL lives in reasoning-based signals: a fine-tuned verifier is a good in-domain tool, but a verifier that generalizes across schemas currently means a large frozen reasoning model.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的中文总结。

# 文本到SQL的正确性预测：一项选择性预测研究

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：文本到SQL系统正被广泛应用，但在关键场景中，错误的查询代价高昂。因此，系统不仅需要生成查询，还需判断何时应该拒绝回答（即选择性预测）。现有方法缺乏对生成查询正确性的可靠估计，尤其是面对复杂多表查询时。
- **核心问题**：哪些信号能够有效预测文本到SQL生成查询的正确性？研究比较了多种不确定性量化信号，包括黑盒统计信号（如自一致性）、白盒对数概率、以及基于LLM的验证器（评判），并探索了训练专用验证器的可能性。
- **整体含义**：本研究表明，正确的预测依赖于**基于推理的验证信号**，而非简单的采样一致性。跨模式泛化目前只能依靠大型冻结推理模型，微调验证器在领域内有效但无法泛化。该研究为实际部署提供了清晰的信号选择指导。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将预测查询正确性视为一个排序问题，使用**AUROC**（ROC曲线下面积）作为主要评估指标，衡量每个信号将正确查询排在错误查询之上的能力。同时，评估信号校准误差（ECE）和选择性预测边界（风险-覆盖曲线）。
- **关键技术细节**：
    - **黑盒统计信号**：包括字符串自一致性（最大相同查询簇大小）、结构自一致性（规范化查询结构聚类）、执行自一致性（结果集聚类）、模式相关性分数（问题和查询使用表/列的嵌入相似度）、查询可执行性（是否能运行并返回行）。
    - **白盒信号**：模型自身产生的平均序列对数概率。
    - **基于LLM的验证器**：将问题、证据、模式、候选SQL一起展示给一个LLM，要求其判断查询是否正确。使用GPT-4o-mini、GPT-4o和Claude-Sonnet-4.6作为评判者。GPT系列使用YES/NO首个token对数概率，Claude使用言语化概率（0-100）。
    - **微调验证器**：在标注的`(问题, 证据, 模式, SQL) -> 正确/错误`对上进行微调，包括特征分类器（基于嵌入、对数概率、自一致性）、微调编码器（ModernBERT-base）和LoRA调优的生成式验证器（Qwen2.5-1.5B/7B）。
    - **选择性预测**：通过校准阈值，在保证目标风险下最大化覆盖率（回答的问题比例），并给出经验边界和分布无关的置信边界（Bonferroni校正）。
- **算法流程**：对每个问题采样K=8个查询，选模态查询（出现最多的）作为待评查询。计算各信号分数，使用交叉拟合逻辑回归组合信号，评估AUROC和校准误差，绘制风险-覆盖曲线。

## 3. 实验设计：数据集、基准与对比方法

- **数据集**：
    - **BIRD**：主数据集，使用800个问题（跨8个数据库），固定切片，模态查询执行准确率为0.451。
    - **Spider**：辅助数据集，使用490个多表开发问题（跨20个数据库），模态准确率0.620。
- **基准**：以执行正确性为标准（生成查询执行结果与人工参考查询匹配）。
- **对比方法**：
    - 黑盒统计信号（字符串/结构/执行自一致性、模式相关性、可执行性）
    - 白盒对数概率
    - LLM验证器（GPT-4o-mini, GPT-4o, Claude-Sonnet-4.6）
    - 双验证器集成（GPT-4o + Claude）
    - 特征分类器、微调编码器、生成式验证器（不同规模）
    - 自修正基线（一个修改循环）
    - 消融实验：如仅使用问题+SQL vs 增加模式和证据。

## 4. 资源与算力

- **论文未明确说明具体GPU型号、数量和训练时长**。文中提到“所有模型输出都已缓存，API实验成本较低”；对于微调实验，使用了ModernBERT-base（编码器）和Qwen2.5-1.5B/7B（生成式），但未给出具体硬件配置。对于大型冻结验证器（GPT-4o, Claude）则依赖API调用。

## 5. 实验数量与充分性

- **实验数量**：涵盖了多个维度：
    - **主要实验**：BIRD数据集上比较10+种信号（表1、表2）。
    - **鲁棒性实验**：更换生成器（GPT-4.1-mini）、更换验证器提供商（Claude）、更换数据集（Spider）、自修正基线（表3）。
    - **验证器训练实验**：包括分布内性能、跨模式转移性能（表4）、多种改进尝试（模式多样性、规模缩放、推理蒸馏、跨基准训练，附录A）。
    - **特征分析**：按查询结构特征（算术、子查询、GROUP BY等）分析错误模式和信号表现（表7）。
    - **校准与选择性预测】实验**：给出校准误差和风险-覆盖曲线（表5、表6、图2）。
- **充分性与公平性**：
    - **充分**：实验覆盖了主数据集、外部数据集、不同生成器、不同提供商验证器、多种训练策略，并提供了统计显著性检验（配对自助法置信区间）。
    - **公平**：所有信号在同一固定切片上评估，使用执行正确性这一客观标准；LLM验证器使用相同提示模板；模版查询被严格定义（模态查询）；交叉拟合逻辑回归避免过拟合。主要发现跨多个设置一致。

## 6. 论文的主要结论与发现

1. **黑盒信号的正确性上限**：字符串/结构/执行自一致性、模式相关性、可执行性、对数概率均处于AUROC 0.61-0.68的狭窄范围，无法有效区分正确与错误查询。
2. **验证信号突破上限**：基于LLM的验证器将AUROC提升至0.72（GPT-4o-mini）和0.78（Claude）。不同提供商裁判错误模式不同，集成两者达到0.82 AUROC，校准误差仅0.03，并支持有效的选择性预测（例如，以24%选择性风险回答27%的问题），而自一致性无法形成任何低风险子集。
3. **训练验证器的局限性**：微调验证器在分布内表现良好（0.77-0.79 AUROC），但在未见模式上转移性能差（降至约0.66）。增加模型规模、模式多样性、推理蒸馏、跨基准训练均未能改善泛化。跨模式转移能力似乎与模型规模和推理能力相关，而非微调。
4. **错误根源**：困难情况主要源于计算和组合（算术、条件逻辑、排序、嵌套等），而非模式链接问题。验证器在这些错误类别上优于自一致性。
5. **自修正无法替代选择性预测**：一个自修止循环几乎不改变准确率（从0.451到0.468），且自我报告置信度严重校准不良（ECE 0.501）。
6. **实践建议**：对于固定部署，可使用微调验证器实现低成本的有用弃权；对于跨模式开放环境，应使用大型冻结推理模型作为验证器，并考虑多提供商集成。

## 7. 优点

- **研究视角独特**：聚焦于选择性预测而非生成改进，清晰地分离了“如何信任查询”与“如何修正查询”两个问题。
- **系统性比较**：提供了同等条件下多种信号的全面对

比，为实践者提供了清晰的信号选择指南。

- **实验设计严谨**：固定切片、配对自助法置信区间、交叉拟合逻辑回归、多种鲁棒性检验，确保了结论的可靠性。
- **实践导向性强**：明确区分了分布内与跨模式场景，给出了具体可行性建议（如集成验证器、微调验证器适用场景等），对实际部署有直接指导作用。
- **错误分析深入**：按查询结构特征（算术、子查询、GROUP BY等）细分错误类型，揭示了验证器与自一致性在不同困难类别上的表现差异，有助于未来研究针对性改进。

## 8. 缺点

- **数据集规模有限**：BIRD仅使用800个问题，Spider仅490个，虽属中等规模，但可能无法完全覆盖真实世界的多样性（如罕见SQL构造、长尾表模式）。
- **生成器单一性**：主实验生成器为GPT-40-mini+等效增强，仅在一次鲁棒性实验中更换为GPT-4.1-mini，未探索其他开源生成器（如CodeLlama、DeepSeek-Coder），可能限制结论泛化性。
- **选择性边界分析不够细致**：仅展示了单一阈值下的风险-覆盖结果，未探索不同目标风险水平下的最优阈值选择，也未讨论拒绝成本不对称（如错误查询代价远大于不回答）的影响。
- **可执行性信号定义模糊**：查询可执行性仅指“能运行并返回行”，未区分“空结果”与“有意义结果”，可能将合法但返回零行的查询误判为错误。
- **训练验证器泛化失败的解释不足**：论文仅指出微调无法跨模式泛化，但未深入分析原因（如是否因训练数据中模式多样性不足、或者是模型容量瓶颈），也未尝试更先进的泛化技术（如元学习、指令跟随微调）。

## 9. 创新点

- **首次系统性对比了三类信号（黑盒、白盒、基于LLM的验证器）在文本到SQL选择性预测任务上的性能**，并确立了“推理验证”的核心作用。
- **提出并验证了双验证器集成的方法**，利用不同提供商错误模式的互补性，实现了AUROC 0.82和低校准误差。
- **构建了完整的训练验证器实验体系**，包括特征分类器、微调编码器、LoRA生成式验证器，并全面测试了分布内与跨模式转移能力，揭示了微调验证器在泛化上的根本局限。
- **提供了实践指导框架**：根据部署环境（固定模式 vs 开放环境）推荐不同方案，将学术发现转化为可操作的技术选型建议。

## 10. 影响与评价

- **学术影响**：该研究为文本到SQL的鲁棒性和可靠性研究开辟了新方向——从“生成更准确查询”转向“知道何时拒绝回答”。其结论直接挑战了“自一致性可以反映正确性”的常见假设，并强调了推理信号的重要性，预计将推动后续工作探索更细粒度的验证器设计或神经符号混合方法。
- **实际应用价值**：对于企业级文本到SQL系统（如客服查询、数据平台自然语言接口），该研究提供了低成本、高可靠性的弃权策略：在领域内场景中，微调一个轻量验证器即可显著降低风险；在跨库场景中，集成大型推理模型API则成为唯一可行方案。这能有效减少误答导致的业务决策失误。
- **局限性社区讨论点**：论文中微调验证器跨模式泛化失败的结果可能引发争议——是否因训练数据量过小（仅1000-2000对）或微调策略不当（如未使用多任务学习）？这需要后续更大规模实验验证。此外，选择性预测的阈值如何自动选择以保证用户期望的风险水平，仍是一个开放问题。

## 11. 个人看法

- **核心启示**：这篇论文最令人印象深刻的发现是“自一致性对正确性预测无效”，这打破了直觉上的“采样一致性越高，答案越可靠”的普遍观念。其根本原因在于文本到SQL的假设空间高度结构化且非线性：往往多个不同SQL都能正确回答同一个问题（例如不同表连接顺序），而单一SQL的局部采样一致性无法反映这种结构的正确性。这提示未来研究应该更关注推理层面的验证，而非统计层面的一致性。
- **实际应用建议**：若需部署文本到SQL系统，推荐优先采用“大型推理模型验证 + 多样化提供商集成”方案，尽管API成本较高，但对可靠性至关重要。若成本受限且数据结构变化不频繁，可考虑微调轻量验证器（如ModernBERT）并定期更新。此外，论文中“自修正”几乎无效的结论强烈建议不要用自生成置信度作为弃权依据，而应将验证与生成分离。
- **未来方向**：值得探索的方向包括：1）利用多轮对话式验证（让LLM一步步检查子步骤）；2）将本文的对比方法扩展到其他生成任务（如代码生成、数学推理）；3）将验证器与生成器联合训练，实现端到端的可靠生成-验证-弃权循环；4）利用少量人工标注的边界案例进行主动学习，提升验证器的泛化性。

（完）
