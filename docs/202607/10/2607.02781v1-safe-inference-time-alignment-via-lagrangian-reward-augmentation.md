---
title: Safe Inference-Time Alignment via Lagrangian Reward Augmentation
title_zh: 通过拉格朗日奖励增强实现安全的推理时对齐
authors: "Yaswanth Chittepu, Ativ Joshi, Sohini Chintala, Scott Niekum"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02781v1"
tags: ["query:smd"]
score: 8.0
evidence: 提出推理时对齐框架，通过拉格朗日方法避免微调，防御有害微调攻击
tldr: 推理时对齐通常只优化单一奖励信号，安全约束需手动调参且易失衡。本文提出拉格朗日奖励增强（LARA）框架，将安全约束通过对偶变量转化为一维凸优化问题，并在小型校准集上求解得到增强奖励。实验显示，LARA在序列级和token级推理方法上显著改善有用性与无害性权衡，其中Best-of-N表现最佳，性能接近微调对齐方法。该工作为无需重训的语言模型安全对齐提供了原则性通用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02781v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1439, \"height\": 450, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02781v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 587, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02781v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 693, \"height\": 532, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02781v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 691, \"height\": 563, \"label\": \"Figure\"}]"
motivation: 现有推理时对齐方法忽略安全约束或依赖手动调参，缺乏原则性约束处理方式。
method: 通过拉格朗日对偶将KL正则化约束目标转化为单变量凸问题，利用小型校准集求解对偶变量并构造增强奖励。
result: LARA在序列级（Best-of-N）和token级推理方法上均提升有帮助性-安全性权衡，Best-of-N接近微调基线。
conclusion: 提出通用推理时安全对齐框架，以轻量校准实现原则性约束处理，平衡有用性与无害性。
---

## 摘要
推理时对齐在解码过程中利用辅助奖励信号来引导冻结的语言模型，避免了重复权重更新的成本。然而，现有的推理时对齐方法通常优化单个标量分数，因此显式安全约束要么被忽略，要么通过手动调整的惩罚项进行编码。我们提出了拉格朗日奖励增强（LARA），这是一个在安全约束下通用的推理时对齐框架。从带有奖励模型和成本模型的KL正则化约束目标开始，LARA对约束进行对偶化，并将优化问题简化为关于非负对偶变量的一维凸问题。在小型校准集上估计的该对偶变量定义了一个增强奖励，可作为现有推理时对齐方法中的即插即用评分信号。对于序列级采样方法（如Best-of-N重排序），校准后的对偶变量对应期望成本约束问题的解。对于令牌级奖励引导解码方法，相同的构造产生了原则性的对偶校准启发式方法，而非精确的约束策略保证。我们在序列级和令牌级推理时对齐方法上评估了LARA，发现LARA改善了有用性与无害性的权衡，其中Best-of-N在推理时方法中达到了最佳性能，接近基于微调的直接对齐基线。

## Abstract
Inference-time alignment steers a frozen language model during decoding using auxiliary reward signals, avoiding the cost of repeated weight updates. However, existing inference-time alignment methods typically optimize a single scalar score, so explicit safety constraints must either be ignored or encoded through manually tuned penalties. We propose Lagrangian Reward Augmentation (LARA), a general inference-time alignment framework under safety constraints. Starting from a KL-regularized constrained objective with a reward model and a cost model, LARA dualizes the constraint and reduces the optimization problem to a one-dimensional convex problem over a nonnegative dual variable. Estimated on a small calibration set, this dual variable defines an augmented reward that can be used as a drop-in scoring signal within existing inference-time alignment methods. For sequence-level sampling methods, such as Best-of-N reranking, the calibrated dual variable corresponds to the solution of the expected-cost constrained problem. For token-level reward-guided decoding methods, the same construction yields a principled dual-calibrated heuristic rather than an exact constrained-policy guarantee. We evaluate LARA on both sequence-level and token-level inference-time alignment methods, and find that LARA improves the helpfulness-harmlessness tradeoff, with Best-of-N achieving the best performance among inference-time methods, approaching finetuning-based direct alignment baselines.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：现有推理时对齐方法（如Best-of-N重排序、令牌级奖励引导解码）通常只优化单一标量奖励分数，无法原则性地处理安全约束。安全约束要么被忽略，要么依赖手动调参的惩罚项，导致有用性与无害性之间的权衡缺乏理论保证。
- **背景**：大语言模型部署中，有用性不足够，还需要避免有害输出。传统安全对齐（如Safe RLHF）通过训练时约束优化实现，但需要昂贵的模型权重重训练，且在不同应用场景或安全需求变化时可能需重复训练。推理时对齐（Inference-time alignment）保持基础模型冻结，在解码时用辅助奖励信号引导生成，成本低，但缺乏处理安全约束的框架。
- **研究意义**：提出一个通用的、原则性的推理时安全对齐框架，将训练时的约束优化转移至解码时，无需更新模型权重，并能即插即用地集成到现有推理时对齐方法中。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：拉格朗日奖励增强（LARA）从Safe RLHF的KL正则化约束目标出发，通过对偶化安全约束，将原始无穷维策略优化问题简化为关于单个非负对偶变量λ的一维凸优化问题。求解该问题得到最优λ，定义增强奖励 rλ(x, y) = r(x, y) - λ c(x, y)，作为安全校准后的评分信号。
- **关键技术细节**：
  - 目标函数：最大化期望奖励 − β KL散度，约束期望成本 ≤ τ。
  - 强对偶性成立（假设Slater条件），对偶函数 g(λ) 有闭式解（定理1）：  
    g(λ) = β E_x[log E_y~πref[exp((r-λc)/β)]] + λτ。
  - 对偶函数g(λ)是凸函数（引理2），其导数 g'(λ) = τ - 期望成本，因此最优λ满足期望成本=τ（若内点）。
  - 校准过程：在小型校准数据集（N个提示，每个提示K个响应样本）上通过蒙特卡洛估计g(λ)，用二分搜索最小化得到估计的λ̂。为稳健性，采用自助法取97.5%分位数。
  - 对于序列级方法（如BoN），校准的λ对应精确期望成本约束；对令牌级方法（如ARGS, CD-Q），提供原则性启发式。
- **理论保证**：定理3给出有限样本保证，当N和K足够大时，估计的λ收敛于总体最优λ*，误差以概率控制。

## 3. 实验设计

- **数据集与场景**：
  - 微调数据集：Alpaca（SFT阶段）。
  - 偏好数据集：BeaverTails（Ji et al. 2023），用于训练奖励模型和成本模型（分别用有帮助性和无害性偏好标签）。
  - 校准数据集：BeaverTails训练集的一个小子集（2000个提示，每个提示20个生成响应）。
  - 评估数据集：BeaverTails测试集（256个测试提示），用于模型评估和GPT评估。
- **基准方法**：
  - 微调基线：SFT（基础模型）、Safe-RLHF、SafeDPO。
  - 推理时基线：λ=0（无安全惩罚），λ>λ*（过度惩罚）。
  - 推理时对齐方法：Best-of-N (BoN) 重排序、ARGS、PARGS、CD-Q。
- **模型**：Llama-3.2-3B、Qwen-2.5-3B。
- **评估方式**：
  - 模型评估：使用预训练的beaver-7b-unified-reward和beaver-7b-unified-cost模型分别评估有帮助性和无害性。
  - GPT评估：用GPT-4o-mini作为法官，按1-10分评级。

## 4. 资源与算力

- **训练算力**：
  - Safe-RLHF：在4个 NVIDIA A100 80GB GPU上训练。
  - SafeDPO、奖励模型、成本模型、部分奖励/成本模型、奖励/成本价值函数：均在2个 NVIDIA A100 80GB GPU上训练。
- **校准与推理**：
  - 使用vLLM生成响应；校准中自助法10,000次约2分钟（因二分搜索高效）。
  - 推理时使用 top-k=50，温度1.2；BoN设置N=20；RGTG方法考虑top-k=50令牌，权重w=2。
- 论文未明确给出训练总时长或校准总耗时，但提及校准快速。

## 5. 实验数量与充分性

- **实验数量**：主要两组实验：
  - 1) λ的敏感性分析（图1）：在两个模型上展示BoN策略的期望奖励和成本随λ变化，验证λ*位于约束边界。
  - 2) 多方法对比（图2、3）：在两个模型上，分别进行模型评估和GPT评估，对比LARA-boN、λ=0、λ>λ*、微调基线及三种RGTG方法（ARGS、PARGS、CD-Q）。
- **充分性与公平性**：
  - 实验覆盖了序列级和令牌级推理方法，以及微调基线，体系较完整。
  - 采用相同测试集、相同训练设置（BeaverTails，与Safe-RLHF/Dai et al.一致），确保比较公正。
  - 消融实验：通过λ的不同取值（0, λ*, >λ*）验证LARA最优性。
  - 但仅使用一个数据集（BeaverTails），两个模型，外部泛化性未验证。令牌级方法（RGTG）在帮助性上表现较差，论文仅归因于方法本身，未做深入分析。评估仅256个提示，样本量有限。
  - GPT评估中BoN对λ不敏感，说明模型奖励/成本比GPT更敏感，这可能影响结论的普遍性。

## 6. 主要结论与发现

- LARA校准的λ*能在满足安全约束（E[c]≤τ）的同时最大化有帮助性，理论最优（图1）。
- 在帮助性-无害性权衡上，BoN+LARA在所有推理时方法中表现最佳，接近微调方法SafeDPO，甚至在某些指标上超过SafeDPO（如帮助性）。
- Safe-RLHF（微调）整体表现最优，但需要权重更新。
- RGTG方法（ARGS, PARGS, CD-Q）在帮助性上远低于SFT基线，尽管无害性较高；CD-Q在RGTG中最好。
- LARA作为解码器无关的校准层，能有效提升现有推理时方法的安全对齐质量，无需重训练。

## 7. 优点

- **通用性**：LARA是解码器无关的校准层，可集成到任何现有推理时对齐方法中（序列级或令牌级）。
- **原则性**：从约束优化出发，通过对偶变量提供理论保证的权衡，避免手动调参。
- **高效**：校准仅需小数据集和快速二分搜索（可甚至10,000次自助约2分钟），推理时仅有轻微计算开销（修改评分函数）。
- **理论保障**：提供有限样本收敛定理，证明校准误差可控。
- **实验验证充分**：对比多种基线（微调与推理时），多评估方式（模型评分+GPT判断），模型多样化。

## 8. 不足与局限

- **令牌级方法的不完美**：LARA的精确期望成本约束解释仅适用于序列级方法（如BoN）；对令牌级方法仅提供启发式，无严格安全保证。实验也显示RGTG方法帮助性显著下降，说明直接应用增强奖励到前缀级评分可能不合适。
- **校准集的分布假设**：假设校准集与部署分布匹配，但实际可能偏移。论文使用自助法上分位数作为保守估计，但未理论分析其鲁棒性。
- **数据集单一**：仅使用BeaverTails数据集，未在更多元或真实红队测试上评估，安全评估的广度不足。
- **计算资源有限**：仅使用3B参数模型，更大模型（7B/13B）上的表现未知。微调基线只在较大模型上可能更有竞争力。
- **评估局限性**：模型评估依赖特定的beaver奖/成本模型，可能存在过拟合；GPT评估中BoN对λ不敏感，说明评估信号不够可靠。测试集仅256个提示，统计显著性可能不足。
- **安全与帮助性冲突**：未考察极端有害情况或"jalbreak"攻击场景，安全约束可能不足。
- **对校准超参数依赖**：BoN的N需至少与校准样本数相当，限制了小N场景下的应用。

（完）
