---
title: A Gravitational Interpretation of Fine-Tuning Reversion
title_zh: 微调回退的引力解释
authors: "Samuele Poppi, Nils Lukas"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.28525v1"
tags: ["query:smd"]
score: 9.0
evidence: 探讨微调导致的安全退化和对齐丢失
tldr: "针对微调无害数据导致安全行为逆转的问题，提出引力解释：早期训练形成主导流形，后续对齐是浅层位移，微调时存在指向主导流形的恢复方向v_rev。实验发现表征漂移迅速获得沿v_rev的分量，对齐度从0.429升至0.647。通过选择性阻断v_rev运动，可将最终对齐度降至-0.211，有害性从19.0%降至8.5%，任务代价小。该工作揭示了历史定义方向在早期恢复动力学中的因果作用，为安全对齐提供新视角。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28525v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1227, \"height\": 557, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28525v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 993, \"height\": 115, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28525v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1163, \"height\": 742, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28525v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1379, \"height\": 387, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28525v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1372, \"height\": 471, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28525v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1377, \"height\": 415, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28525v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1102, \"height\": 320, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28525v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 651, \"height\": 243, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28525v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1430, \"height\": 733, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28525v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 968, \"height\": 338, \"label\": \"Table\"}]"
motivation: 微调无害数据会意外逆转安全对齐行为，需要统一解释其动力学机制。
method: 提出引力假设，识别历史定义的主导流形恢复方向v_rev，并通过阻断实验验证其因果作用。
result: "v_rev对齐度随更新快速上升，阻断v_rev运动可将有害性从19.0%降至8.5%，几乎不影响任务性能。"
conclusion: v_rev是早期后对齐恢复的关键因果中介，但并非唯一安全方向，其作用受限于当前实验设置。
---

## 摘要
对无害数据进行微调可以部分撤销训练早期习得的行为。安全可能在良性后对齐更新中侵蚀，已遗忘的能力可能重新出现，潜在特征可能通过明显无关的监督转移，相关的后对齐脆弱性也出现在其他生成设置中。我们认为通过一个共同的训练历史视角来看待这些现象是有益的。我们的假设是几何性的：早期的大规模训练阶段创建了主导的行为流形，而后期的对齐或专业化阶段则是从这些流形的较浅位移。因此，后续的微调可以继承一个持久的回退分量，指向主导流形的一个见证。我们称此为微调回退的引力解释。在我们的主要设置中，表征漂移迅速获得一个沿历史定义的回退方向（v_rev）的分量。在我们的主轨迹中，与v_rev的对齐从第一次更新后的cos=0.429±0.052上升到第20步时的0.647±0.021。在24个运行-步数对中，每个观测到的对齐都超过了各向同性激活空间零假设的p99。我们证明，选择性阻止沿v_rev的运动将T=100时的最终对齐从0.648±0.009改变为-0.211±0.021，并将有害性从19.0%±4.0%降低到8.5%±1.5%，且任务成本很小。这些结果支持v_rev作为我们设置中对齐后早期回退的因果相关中介。重要的是，我们并不声称v_rev是唯一的安全方向，也不声称主导流形被直接观察到；相反，我们识别出一个鲁棒的、由历史定义的方向，它解释并部分控制了早期回退动态。

## Abstract
Fine-tuning on harmless data can partially undo behaviors acquired earlier in training. Safety can erode under benign post-alignment updates, unlearned capabilities can re-emerge, latent traits can transfer through apparently unrelated supervision, and related post-alignment fragility appears in other generative settings. We argue these phenomena are usefully viewed through a common training-history lens. Our hypothesis is geometric: large early training phases create dominant behavioral manifolds, while later alignment or specialization phases are shallower displacements from them. Subsequent fine-tuning can therefore inherit a persistent reversion component pointing back toward a witness of the dominant manifold. We call this the gravitational interpretation of fine-tuning reversion. Across our main settings, representational drift rapidly acquires a component along a history-defined reversion direction (v_rev). In our main track, alignment with v_rev rises from cos = 0.429 +/- 0.052 after the first update to 0.647 +/- 0.021 by step 20. Across 24 run-step pairs, every observed alignment exceeds the p99 of an isotropic activation-space null. We demonstrate that selectively blocking motion along v_rev changes the final alignment at T=100 from 0.648 +/- 0.009 to -0.211 +/- 0.021 and reduces harmfulness from 19.0% +/- 4.0% to 8.5% +/- 1.5% with little task cost. These results support v_rev as a causally relevant mediator of early post-alignment reversion in our setup. Importantly, we do not claim that v_rev is the unique safety direction, nor that the dominant manifold is directly observed; rather, we identify a robust, history-defined direction that explains and partially controls early reversion dynamics.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：微调无害数据（如普通指令数据）会导致安全对齐模型的行为逆转（如产生有害输出）、已遗忘能力重新出现、潜在特征通过无关监督转移等现象，现有工作缺乏统一的机制解释。
- **背景**：已有研究表明安全对齐仅占据浅层低秩子空间、存在有限局部安全盆地；不同任务（如安全、代码）的微调均表现出回退行为。
- **核心含义**：作者提出“引力解释”——早期大规模训练（预训练+通用助理调优）形成主导行为流形（helpful 区域），后期对齐或专业化阶段仅是浅层位移。后续良性微调优化时，除了任务梯度外，还存在一个指向早期主导流形的恢复分量（回退方向 `v_rev`），导致安全侵蚀等现象。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：在激活空间中，定义**回退方向** `v_rev` 为从对齐后起始模型 `θ_S` 到“有帮助见证”检查点 `θ_H` 的位移向量（使用有害提示探针族 P 的平均残差流激活）：
  ```
  v_rev(ℓ) = m_θH(P, ℓ) - m_θS(P, ℓ)
  ```
- **关键假设**：
  1. 早期大规模训练形成的主导流形具有不对称的历史预算优势（`B_pre >> B_helpful >> B_safety >> B_finetune`）。
  2. 后续微调的梯度可分解为任务分量 `g_task` 和回退分量 `g_rev`（指向主导流形）。
  3. 通过选择性抑制或放大沿 `v_rev` 的运动，应能改变下游行为（因果预测）。
- **关键技术细节**：
  - 使用**有害提示**（AdvBench 的 16 个目标）作为探针族 P 来测量激活空间位移。
  - 通过**平方 ReLU 屏障**（`L_block = L_task + λ·ReLU(p(θ))²`）抑制沿 `v_rev` 的正向漂移，用**线性目标**（`L_push = L_task - λ·p(θ)`）放大漂移，其中 `p(θ) = ⟨m_θ(P)-m_θS(P), v_rev_hat⟩`。
  - 选择晚期层（如 Llama-3.1-8B 的 L=31）作为摘要层，因其拒绝方向幅度最大。
- **算法流程**（文字说明）：
  1. 从基础模型构建 6 个“有帮助见证”检查点（LoRA 与全微调 × 3 种子），验证其一致性。
  2. 固定一个典型见证，定义 `v_rev`。
  3. 在良性微调过程中，每隔几步提取激活向量，计算与 `v_rev` 的余弦对齐度。
  4. 进行因果干预：在微调损失中加入几何约束（阻断或推动），观察对齐度与有害率的变化。

### 3. 实验设计：使用的数据集/场景、benchmark、对比方法

- **主要模型**：Llama-3.1-8B（安全对齐版本 `θ_S`，基础版用于构建见证）。
- **支持模型**：Llama-3.2-3B、Qwen2.5-3B。
- **下游良性微调数据集**：Alpaca（通用指令）、HumanEvalPack Python（代码）、GSM8K（数学）。
- **见证构建数据集**：OASST2（英语 rank-0 对话）、HH-RLHF helpful（有用回答）、HumanEvalPack Python（代码），过滤掉拒绝式语言，混合上限 7000 条。
- **评估基准**：
  - **安全性**：BeaverTails 有害率（使用 Llama-Guard-3-8B 作为评判器，部分实验补充 Qwen3Guard-Gen-8B）。
  - **任务性能**：保留集困惑度（Alpaca、GSM8K 等）。
  - **几何度量**：余弦对齐度、参与比（PCA 降维评估方向子空间维度）。
- **对比方法**：
  - 基线（正常微调，全 FT 或 LoRA）。
  - 阻断 `v_rev`（`block-v_rev`）。
  - 阻断随机方向（`block-v_rand`，5 个随机方向取平均）。
  - 推动 `v_rev`（`push-v_rev`，探索性）。
  - 跨任务比较（Alpaca vs Code 的几何对齐）。
  - 代码专业化起始点实验（从代码专化模型开始微调）。

### 4. 资源与算力

- 论文提到所有训练和激活提取均使用 **Hugging Face transformers 模型**，**bfloat16 精度**，**单 GPU** 完成。
- **未明确说明** GPU 型号、具体数量（除“单 GPU”外）以及总训练时长（仅给出步数，如 50 步见证训练、100 步下游微调等）。附录提及某些运行使用批大小 2 或 4，8-bit AdamW 等。总体算力规模相对较小，属于单卡可完成的实验。

### 5. 实验数量与充分性

- **主要实验组**：
  - 几何时间过程：24 个运行-步数对（2 种子 × 3 数据集 × 4 步数：1,5,20,100），每个步数均验证超过各向同性零假设 p99。
  - 见证一致性：6 个构建的见证（LoRA/全 FT × 3 种子），计算 pairwise 余弦和参与比。
  - 跨任务收敛：9 个良性全微调运行（3 数据集 × 3 种子），分析激活方向低秩性。
  - 行为耦合：20 个长时检查点（4 运行 × 5 步数），Spearman r=0.877。
  - 因果干预：主 8B 设置（2 种子），block-v_rev、block-v_rand（5 方向 × 2 种子）、push-v_rev（1 种子）；支持模型各 2 种子。
  - 代码专业化起点实验（2 下游 × 2 种子）。
- **充分性评估**：
  - 实验覆盖了**不同模型规模**（8B、3B）、**不同更新机制**（LoRA vs 全 FT）、**不同下游任务**（指令、代码、数学）、**不同起始点**（安全对齐 vs 代码专业化），并进行了**随机方向对照**和**跨模型验证**。
  - 同时使用两个评判器（Llama-Guard-3-8B 和 Qwen3Guard-Gen-8B）验证安全性趋势的鲁棒性。
  - 论文也坦承**局限**：主要结果依赖单一模型族（Llama-3.1-8B）和单一评判器（主文本）；干预限于早期（T≤100）；行为耦合分析仅基于少量时间点，为相关性而非因果性。整体实验设计相对充分且客观，未发现明显不公平对比。

### 6. 论文的主要结论与发现

1. **几何发现**：后续微调迅速获得到达 `v_rev` 的强对齐分量（cos 从 0.429 升至 0.647），且所有步数均超过各向同性零假设 p99；该方向在多个见证下高度一致（pairwise cos=0.82，参与比 1.36/6）。
2. **跨任务收敛**：不同良性任务（Alpaca、Code）的权重更新几乎正交，但有害提示的激活位移高度对齐（cos=0.79），表明存在共享的回退几何结构。
3. **行为耦合**：在基线运行中，与 `v_rev` 的对齐度与有害率呈强正相关（Spearman r=0.877），且安全性退化在早期即出现。
4. **因果相关**：阻断沿 `v_rev` 的运动可将最终对齐度从 0.648 降至 -0.211，有害率从 19.0% 降至 8.5%（接近对齐锚点 4.0%），且任务困惑度几乎不变；随机方向控制无效，推动方向则大幅提升有害率（59.0%）。
5. **非安全专用性**：从代码专业化起点出发，后续微调仍存在指向有帮助见证的回退方向（cos≈0.5），表明该现象不限于安全对齐上下文。
6. **模型泛化**：在 Llama-3.2-3B 和 Qwen2.5-3B 上得到定性一致的结果。

### 7. 优点：方法或实验设计上的亮点

- **统一框架**：将安全退化、能力恢复、潜特征转移等现象纳入共同的“训练历史不对称”视角，提出可检验的几何假设。
- **因果干预设计**：不仅观测相关性，还通过直接操控 `v_rev` 分量验证因果关系，并使用随机方向对照，增强了结论的稳健性。
- **几何与行为的结合**：在激活空间定义具体方向，并与有害率变化进行了量化关联（Spearman 相关系数），而非仅依赖定性观察。
- **跨模型、跨任务、跨更新机制验证**：从 LoRA 到全 FT，从指令到代码，从 8B 到 3B，展示了现象的广泛性。
- **公开数据集与详细附录**：超参数、探针列表、评判器细节均给出，有利于复现。

### 8. 不足与局限

- **主要依赖单一模型族**：核心几何与行为耦合结果基于 Llama-3.1-8B，虽在 3B 模型上有支持性复现，但泛化性仍需更多模型验证。
- **早期干预局限**：因果干预仅在 T≤100 步（早期阶段）验证，长期（如 500、1000 步）效果未知。
- **代理流形限制**：帮助见证仅是对主导流形的局部近似，而非直接观测到流形本身，因此解释仍属推断性。
- **行为耦合的弱因果性**：几何-行为相关性基于较少数量的时间点，虽之后有干预佐证，但未完全建立完整因果链条。
- **未完全参数化回退强度**：论文识别了方向结构，但未定量描述回退强度如何随训练历史不对称性（如预训练规模、对齐阶段大小）变化。
- **评判器偏差风险**：主文本使用 Llama-Guard-3-8B，附录虽补充 Qwen3Guard 但绝对标度不同，结论对评判器的依赖需进一步检验。
- **引力隐喻的过度解释风险**：作者自行指出不应字面理解为物理定律，仅为历史诱导偏好的几何描述。

（完）
